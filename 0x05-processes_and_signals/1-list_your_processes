#!/usr/bin/env bash
# list all currently running processes

echo "=== All Running Processes (User-Oriented View) ==="
ps -eo user,pid,ppid,%cpu,%mem,tty,cmd --sort=ppid | column -t

echo ""
echo "=== Process Hierarchy (Parent-Child Relationships) ==="
pstree -pau
